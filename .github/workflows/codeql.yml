name: CodeQL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: "17 7 * * 1"

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      security-events: write
      contents: read
      actions: read
    strategy:
      fail-fast: false
      matrix:
        language: [ python, javascript-typescript ]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: github/codeql-action/init@9e8d0789e6be7f7eb080397a528c79fb0d3e7c5e # v3.28.9
        with:
          languages: ${{ matrix.language }}
      - uses: github/codeql-action/analyze@9e8d0789e6be7f7eb080397a528c79fb0d3e7c5e # v3.28.9
        with:
          category: "/language:${{ matrix.language }}"
